export enum CustomerGender {
    male = 1,
    female = 2,
    other = 0,
}

export enum UserRole {
    customer = 1,
    store = 2,
    administrator = 0,
}

export enum UserStatus {
    Activated = 0,
    Deleted = 1,
    Approved = 2,
}